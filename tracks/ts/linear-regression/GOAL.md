# Goal

## Goal

The goals here are to learn about
    - linear regression
    - gradient descent

### Acceptance criteria

A TS program that..

- [x] Parses the CSV data
- [x] Does some simple data handling (= Standardizes rows with high variability)
- [ ] Builds a linear regression model through gradient descent
- [ ] Evaluates the performance of the model (MSE) occassionally during training
- [ ] Computes a few important metrics about the model performance

### Skills practiced

- Linear regression
- Model evaluation (MSE)
- Gradient descent

## Artifacts produced

- session output in `artifacts/session.txt`